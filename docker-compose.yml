services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: grading_db
      POSTGRES_USER: grading_user
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U grading_user -d grading_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://grading_user:${POSTGRES_PASSWORD}@postgres:5432/grading_db
      SECRET_KEY: ${SECRET_KEY:?Set SECRET_KEY in .env}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./backend:/app
      - submission_data:/data/submissions
    command: ["sh", "-c", "alembic upgrade head && exec uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"]

  sandbox-image:
    build:
      context: ./sandbox
    image: ai-grading-sandbox:latest
    command: ["true"]

  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql://grading_user:${POSTGRES_PASSWORD}@postgres:5432/grading_db
      SECRET_KEY: ${SECRET_KEY:?Set SECRET_KEY in .env}
      SUBMISSION_STORAGE_PATH: /data/submissions
      GRADING_SANDBOX_IMAGE: ai-grading-sandbox:latest
    depends_on:
      postgres:
        condition: service_healthy
      sandbox-image:
        condition: service_completed_successfully
    volumes:
      - ./backend:/app
      - submission_data:/data/submissions:ro
      - /var/run/docker.sock:/var/run/docker.sock
    command: ["sh", "-c", "alembic upgrade head && exec python -m app.worker"]

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - backend
    environment:
      VITE_API_URL: http://localhost:8000/api/v1

volumes:
  postgres_data:
  submission_data:
