[project]
name = "ai-assisted-code-grading-backend"
version = "0.1.0"
description = "Backend for AI-assisted programming assignment grading platform"
requires-python = ">=3.11"

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]
